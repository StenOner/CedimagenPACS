import { TranslateUserStatePipe } from './translate-user-state.pipe';

describe('TranslateUserStatePipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateUserStatePipe();
    expect(pipe).toBeTruthy();
  });
});
